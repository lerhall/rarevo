<div className="flex gap-4 overflow-x-auto snap-x snap-mandatory pb-4">
  {cards.map((card) => (
    <div key={card.id} className="snap-start shrink-0 w-48">
      {/* CardTile content */}
    </div>
  ))}
</div>